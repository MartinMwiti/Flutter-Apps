// class is a blueprint for an object i.e it's properties
class Quote {
  // property
  String text;
  String author;

  // contructor(named parameter) - takes in values. SHORTER
  Quote({this.text, this.author});
}
